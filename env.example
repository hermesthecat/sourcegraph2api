# Sunucu Ayarları
PORT=7033
NODE_ENV=production

# Oturum (Session) için Gerekli Gizli Anahtar
# Lütfen burayı kendi karmaşık ve tahmin edilemez anahtarınızla değiştirin.
# openssl rand -base64 32 komutu ile güvenli bir anahtar üretebilirsiniz.
SESSION_SECRET=s2a-super-secret-key-please-change-me

# ===== Opsiyonel Ayarlar =====

# Detaylı loglamayı açar (true/false)
DEBUG=false

# İstek Limiti (dakikada bir IP adresi için)
REQUEST_RATE_LIMIT=60

# API rotaları için genel ön ek (örn: /api)
# ROUTE_PREFIX=/api

# Sourcegraph'a yapılan istekler için HTTP/HTTPS proxy adresi
# PROXY_URL=http://user:pass@host:port

# Sunucuya erişimi engellenecek IP adresleri (virgülle ayrılmış)
# IP_BLACKLIST=1.1.1.1,2.2.2.2