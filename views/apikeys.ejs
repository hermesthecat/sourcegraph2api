<!DOCTYPE html>
<html lang="tr">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>API Anahtar Yönetimi | Sourcegraph Proxy</title>
  <link rel="stylesheet" href="/style.css">
</head>

<body>
  <div class="container">
    <%- include('partials/nav.ejs') %>
      <%- include('partials/messages.ejs') %>

        <h1>API Key Yönetimi</h1>

        <div class="content-grid">
          <div class="form-container">
            <h2>API Key Oluştur</h2>
            <form action="/admin/apikeys/add" method="POST">
              <div class="form-group">
                <label for="alias">Takma Ad (Alias)</label>
                <input type="text" id="alias" name="alias" placeholder="Örn: Mobil Uygulama Anahtarı" required>
              </div>
              <button type="submit" class="btn btn-primary">Ekle</button>
            </form>
          </div>

          <h2>Mevcut API Key'leri (<%= apiKeys.length %>)</h2>
          <table>
            <thead>
              <tr>
                <th>Takma Ad</th>
                <th>API Key</th>
                <th>Durum</th>
                <th>Oluşturulma Tarihi</th>
                <th>Eylemler</th>
              </tr>
            </thead>
            <tbody>
              <% if (apiKeys.length> 0) { %>
                <% apiKeys.forEach(apiKey=> { %>
                  <tr>
                    <td>
                      <%= apiKey.alias %>
                    </td>
                    <td><code><%= apiKey.key %></code></td>
                    <td>
                      <% if (apiKey.isActive) { %>
                        <span class="status status-active">Aktif</span>
                        <% } else { %>
                          <span class="status status-passive">Pasif</span>
                          <% } %>
                    </td>
                    <td>
                      <%= new Date(apiKey.createdAt).toLocaleString('tr-TR') %>
                    </td>
                    <td class="actions">
                      <form action="/admin/apikeys/toggle/<%= apiKey.id %>" method="POST">
                        <button type="submit" class="btn <%= apiKey.isActive ? 'btn-secondary' : 'btn-success' %>">
                          <%= apiKey.isActive ? 'Pasif Yap' : 'Aktif Yap' %>
                        </button>
                      </form>
                      <form action="/admin/apikeys/delete/<%= apiKey.id %>" method="POST"
                        onsubmit="return confirm('Bu API Key\'yi silmek istediğinizden emin misiniz? Bu Key\'i kullanan tüm uygulamaların erişimi kesilecektir.');">
                        <button type="submit" class="btn btn-danger">Sil</button>
                      </form>
                    </td>
                  </tr>
                  <% }) %>
                    <% } else { %>
                      <tr>
                        <td colspan="5" style="text-align: center;">Henüz hiç API Key eklenmemiş.</td>
                      </tr>
                      <% } %>
            </tbody>
          </table>
        </div>
  </div>
</body>

</html>