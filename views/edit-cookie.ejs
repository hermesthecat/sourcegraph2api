<!DOCTYPE html>
<html lang="tr">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Cookie Düzenle | Sourcegraph Proxy</title>
  <link rel="stylesheet" href="/style.css">
</head>

<body>
  <%- include('partials/nav.ejs') %>
  <main class="container">
    <div class="page-header">
      <h1>Cookie'yi Düzenle</h1>
    </div>
    <%- include('partials/messages.ejs') %>

    <div class="card">
      <form action="/admin/cookies/edit/<%= cookie.id %>" method="POST" class="form-container">
        <div class="form-group">
          <label for="alias">Takma Ad (Alias)</label>
          <input type="text" id="alias" name="alias" value="<%= cookie.alias %>" required>
        </div>
        <div class="form-group">
          <label for="cookieValue">Cookie Değeri (SG_COOKIE)</label>
          <textarea id="cookieValue" name="cookieValue" required rows="5"><%= cookie.cookieValue %></textarea>
        </div>
        <button type="submit" class="btn btn-primary">Güncelle</button>
        <a href="/admin/cookies" class="btn" style="background-color: #7f8c8d; color: white;">İptal</a>
      </form>
    </div>
  </main>
</body>

</html>