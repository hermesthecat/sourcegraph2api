<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Sourcegraph API Key | Sourcegraph Proxy</title>
  <link rel="stylesheet" href="/style.css">
</head>

<body>
  <div class="container">
    <%- include('partials/nav') %>
      <%- include('partials/messages') %>

        <h1>Edit Sourcegraph API Key</h1>

        <div class="form-container">
          <form action="/admin/cookies/edit/<%= cookie.id %>" method="POST">
            <div class="form-group">
              <label for="alias">Alias</label>
              <input type="text" id="alias" name="alias" value="<%= cookie.alias %>" required>
            </div>
            <div class="form-group">
              <label for="cookieValue">Sourcegraph API Key Value</label>
              <textarea id="cookieValue" name="cookieValue" required rows="5"><%= cookie.cookieValue %></textarea>
            </div>
            <button type="submit" class="btn btn-primary">Update</button>
            <a href="/admin/cookies" class="btn btn-secondary">Cancel</a>
          </form>
        </div>
  </div>
</body>

</html>