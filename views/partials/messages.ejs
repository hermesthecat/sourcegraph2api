<% if (messages.error) { %>
    <div class="alert alert-error">
        <p>
            <%= messages.error %>
        </p>
    </div>
    <% } %>

        <% if (messages.success) { %>
            <div class="alert alert-success">
                <p>
                    <%= messages.success %>
                </p>
            </div>
            <% } %>