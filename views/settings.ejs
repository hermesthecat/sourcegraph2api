<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ayarlar - Yönetim Paneli</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/style.css">
</head>

<body>
    <div class="container">
        <%- include('partials/nav') %>
            <%- include('partials/messages') %>

                <div class="card">
                    <div class="card-header">
                        <h2>Uygulama Ayarları</h2>
                    </div>
                    <div class="card-body">
                        <p class="card-text">
                            Bu ayarlar veritabanında saklanır ve anında güncellenir.
                            <strong>Kaydettikten sonra sunucuyu yeniden başlatmanıza gerek yoktur.</strong>
                        </p>
                        <form action="/admin/settings" method="POST">

                            <div class="mb-3">
                                <label for="sessionSecret" class="form-label">Oturum Gizli Anahtarı (Session
                                    Secret)</label>
                                <input type="text" class="form-control" id="sessionSecret" name="sessionSecret"
                                    value="<%= settings.sessionSecret %>" required>
                                <div class="form-text">Kullanıcı oturumlarının güvenliği için kullanılan karmaşık bir
                                    anahtar.</div>
                            </div>

                            <div class="mb-3">
                                <label for="requestRateLimit" class="form-label">İstek Limiti (Dakikada)</label>
                                <input type="number" class="form-control" id="requestRateLimit" name="requestRateLimit"
                                    value="<%= settings.requestRateLimit %>" required>
                                <div class="form-text">Bir IP adresinin bir dakika içinde yapabileceği maksimum istek
                                    sayısı.</div>
                            </div>

                            <div class="mb-3">
                                <label for="routePrefix" class="form-label">API Rota Ön Eki (Route Prefix)</label>
                                <input type="text" class="form-control" id="routePrefix" name="routePrefix"
                                    value="<%= settings.routePrefix %>">
                                <div class="form-text">Tüm API yollarının önüne eklenecek genel ön ek (örn: /api). Boş
                                    bırakılabilir.</div>
                            </div>

                            <div class="mb-3">
                                <label for="proxyUrl" class="form-label">Proxy Adresi</label>
                                <input type="text" class="form-control" id="proxyUrl" name="proxyUrl"
                                    value="<%= settings.proxyUrl %>">
                                <div class="form-text">Sourcegraph'a yapılan istekler için kullanılacak HTTP/HTTPS proxy
                                    adresi. (örn: http://user:pass@host:port)</div>
                            </div>

                            <div class="mb-3">
                                <label for="ipBlacklist" class="form-label">IP Kara Listesi</label>
                                <textarea class="form-control" id="ipBlacklist" name="ipBlacklist"
                                    rows="3"><%= settings.ipBlacklist %></textarea>
                                <div class="form-text">Sunucuya erişimi engellenecek IP adresleri (virgülle ayırarak
                                    yazınız).</div>
                            </div>

                            <div class="mb-3">
                                <label for="logLevel" class="form-label">Log Seviyesi</label>
                                <select class="form-select" id="logLevel" name="logLevel">
                                    <option value="error" <%=settings.logLevel==='error' ? 'selected' : '' %>>Error
                                    </option>
                                    <option value="warn" <%=settings.logLevel==='warn' ? 'selected' : '' %>>Warning
                                    </option>
                                    <option value="info" <%=settings.logLevel==='info' ? 'selected' : '' %>>Info
                                    </option>
                                    <option value="debug" <%=settings.logLevel==='debug' ? 'selected' : '' %>>Debug
                                    </option>
                                </select>
                                <div class="form-text">Uygulamanın ne kadar detaylı log tutacağını belirler.</div>
                            </div>

                            <div class="mb-3">
                                <label for="userAgent" class="form-label">User-Agent</label>
                                <input type="text" class="form-control" id="userAgent" name="userAgent"
                                    value="<%= settings.userAgent %>">
                                <div class="form-text">Sourcegraph'a yapılan isteklerde gönderilecek User-Agent başlığı.
                                </div>
                            </div>

                            <div class="mb-3">
                                <label for="tz" class="form-label">Zaman Dilimi (Time Zone)</label>
                                <input type="text" class="form-control" id="tz" name="tz" value="<%= settings.tz %>">
                                <div class="form-text">Uygulama genelinde kullanılacak zaman dilimi (örn:
                                    Europe/Istanbul, UTC).</div>
                            </div>

                            <div class="mb-3">
                                <label for="reasoningHide" class="form-label">Reasoning Gizle</label>
                                <select class="form-select" id="reasoningHide" name="reasoningHide">
                                    <option value="true" <%=settings.reasoningHide===true ? 'selected' : '' %>>Evet
                                    </option>
                                    <option value="false" <%=settings.reasoningHide===false ? 'selected' : '' %>>Hayır
                                    </option>
                                </select>
                                <div class="form-text">Modelin düşünme sürecini (reasoning) yanıttan gizleyip
                                    gizlemeyeceğini belirler.</div>
                            </div>
                            <div class="mb-3">
                                <label for="sourcegraphBaseUrl" class="form-label">Sourcegraph Base URL</label>
                                <input type="text" class="form-control" id="sourcegraphBaseUrl"
                                    name="sourcegraphBaseUrl" value="<%= settings.sourcegraphBaseUrl %>" required>
                                <div class="form-text">Sourcegraph API'sinin temel URL'si (örn:
                                    https://sourcegraph.com).</div>
                            </div>

                            <div class="mb-3">
                                <label for="chatEndpoint" class="form-label">Chat Endpoint</label>
                                <input type="text" class="form-control" id="chatEndpoint" name="chatEndpoint"
                                    value="<%= settings.chatEndpoint %>" required>
                                <div class="form-text">Sourcegraph chat API'sinin endpoint yolu (örn:
                                    /.api/completions/stream).</div>
                            </div>
                            <button type="submit" class="btn btn-primary">Ayarları Kaydet</button>
                        </form>
                    </div>
                </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>